# Decisions Log

## 2026-03-01

### D-001: 문서를 SSOT로 운영
- 배경: 구현과 기획 간 불일치 방지 필요
- 결정: `docs/` 9개 문서를 먼저 정의하고 구현 시 직접 매핑
- 영향: 이후 기능 추가 시 문서 업데이트가 선행 조건

### D-002: 하루 1개 미션 정책
- 배경: 번아웃 사용자에게 다중 목표는 진입 장벽이 큼
- 결정: 날짜 기반 deterministic 방식으로 하루 1개 미션만 제시
- 영향: 같은 날 재방문 시 동일 미션 유지

### D-003: 저장소는 localStorage(v1)
- 배경: 로그인 없는 MVP에서 즉시 동작 필요
- 결정: `healing-site:v1` 키로 완료 기록/누적 카운트 저장
- 영향: 브라우저 단위 데이터 유지, 서버 연동은 다음 단계로 이관

### D-004: 최근 30일 기록 고정
- 배경: 요구사항 7~30일 범위에서 최대치 기본값 필요
- 결정: `/history` 기본 표시를 최근 30일로 고정
- 영향: 추가 필터(7일/30일 토글)는 백로그로 분리

### D-005: 테스트 전략
- 배경: MVP 일정 내 빠른 검증 필요
- 결정: UI E2E 대신 유틸 로직 중심 Vitest 샘플 테스트 적용
- 영향: 핵심 deterministic/중복 완료 방지/히스토리 길이 규칙을 자동 검증

### D-006: MVP Must/Should/Could 확정
- 배경: 구현 착수 전 범위 변동을 줄이고 역할 간 핸드오프 기준 필요
- 결정: Must(핵심 사용자 여정 완성), Should(품질/운영 고도화), Could(확장 기능)로 확정
- 영향: 출시 판단은 Must 완료 기준, Should/Could는 일정 여유 시 순차 반영

### D-007: MVP 성공지표 3개 확정
- 배경: 기능 구현 완료와 사용자 가치 전달을 동일 기준으로 검증할 필요
- 결정: 핵심 여정 완료율 95%+, 저장 유지율 100%, 즉시 피드백 노출률 100%를 KPI로 채택
- 영향: QA/데모/릴리즈 체크리스트가 해당 지표를 통과해야 MVP 완료로 판단

## 2026-03-02

- D-008: Vision 문서 구조 고정
  - 결정: 타깃, 문제 정의(1문장), 서비스 약속(1문장), 핵심 가치(3개) 구조를 PO 기준 템플릿으로 확정
  - 영향: 이후 기획 변경 시에도 동일 구조로 문서 갱신해 팀 간 해석 차이를 줄임
- D-009: PRD MoSCoW 기준 확정
  - 결정: Must/Should/Could를 출시 판정(필수/권장/백로그) 기준으로 사용
  - 영향: MVP 완료 판단은 Must 충족 여부로만 결정하고 Should/Could는 릴리즈 게이트에서 분리
- D-010: 비기능 요구 우선순위 확정
  - 결정: 모바일(360px, 터치 44x44)과 접근성(WCAG AA, 키보드 포커스, 명명) 요구를 MVP 필수 품질 기준으로 채택
  - 영향: UX/FE 산출물과 QA 체크리스트에 동일 기준을 강제 적용
- D-011: 측정 기준은 당분간 로컬(익명)로 고정
  - 배경: MVP 단계에서는 외부 분석 도구 도입보다 지표 정의와 측정 일관성 확보가 우선
  - 결정: KPI 측정은 `localStorage` 기반 익명 이벤트(`anonymous_user_id`)로 수행하고 Supabase/GA 등 외부 도구 도입 결정은 보류
  - 영향: 지표 산출은 로컬 이벤트 로그 기준으로 진행하며 외부 연동 아키텍처 결정은 다음 단계 백로그로 이관
- D-012: MVP 성공지표를 행동지표 3개로 재정의
  - 배경: "힐링 루프 형성" 여부를 사용자 행동 데이터로 검증할 필요
  - 결정: Day1 미션 완료율, 7일 내 재방문율, 호흡(60초) 시작/완료율을 MVP 핵심 지표로 채택
  - 영향: 릴리즈 후 평가 기준은 기능 완료 여부보다 행동지표 추이를 우선 확인
